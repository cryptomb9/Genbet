[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[start]
cmd = "pnpm --filter @workspace/bot run start"
